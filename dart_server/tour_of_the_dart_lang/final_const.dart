main() {
  
  final nickName = 'ryonryon';
  final String name = 'ryo';

  print(nickName);
  print(name);

  const bar = 100000;
  const double atm = bar * 50.3;

  print(atm);

  var foo = const [];
  final bar2 = const [];
  const baz = []; // Equivalent to `const []`

  foo = [1, 2, 3];

  print(foo);
  print(bar2);

  // Built-in types

  // numbers
  // strings
  // booleans
  // lists (also known as arrays)
  // sets
  // maps
  // runes (for expressing Unicode characters in a string)
  // symbols
}
